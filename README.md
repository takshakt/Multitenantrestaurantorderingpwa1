
  # Multi-Tenant Restaurant Ordering PWA

  This is a code bundle for Multi-Tenant Restaurant Ordering PWA. The original project is available at https://www.figma.com/design/SP4LbCS8yyWqtuR3afB9fn/Multi-Tenant-Restaurant-Ordering-PWA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  